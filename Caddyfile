:3000 {
	# Uso local fuera de Docker: ejecutar Caddy desde la raiz del repo
	# para que ./Frontend sea una ruta valida.
	root * ./Frontend
	file_server
	
	# Proxy reverso: redirigir todas las peticiones /api/* al backend Go
	reverse_proxy /api/* localhost:4006
}