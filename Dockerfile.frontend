# Dockerfile para el frontend con Caddy
FROM caddy:2-alpine

# Copiar archivos del frontend
COPY Frontend/ /srv

# Copiar configuración de Caddy para Docker
COPY Caddyfile.docker /etc/caddy/Caddyfile

# Exponer puerto
EXPOSE 80

# Caddy se ejecuta automáticamente